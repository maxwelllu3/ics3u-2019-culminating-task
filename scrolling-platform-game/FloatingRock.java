import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class FloatingRock here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class FloatingRock extends Platform
{
    /**
     * Constructor
     * 
     * Called once when object is created.
     */
    FloatingRock(int scrollableWorldX, int scrollableWorldY)
    {
        super(scrollableWorldX, scrollableWorldY);
    }
    
    /**
     * Act - do whatever the FloatingRock wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
}
